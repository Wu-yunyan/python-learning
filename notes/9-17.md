# 2026-09-17 notes

- 学了：grep 参数（-i 忽略大小写 / -v 反选 / -n 带行号 / -c 只数条数 / -r 递归搜目录）；管道 `|` 把左边输出喂给右边；重定向 `>` 覆盖写、`>>` 追加写

- 卡过：**Ctrl+Z 是挂起不是终止**。卡住的命令用 `jobs` 看编号 → `kill %N` 清掉；要终止用 `Ctrl+C`;`/` 开头从根目录数，不带 `/` 从当前目录数