# 2026-09-21 notes

- 学了：端口常识（22/80/443/3306/3389/445/8080）+ HTTPS/TLS 握手 + 三次握手四次挥手
- 实操：find 三段递进（-name → -type → -perm -4000）+ auth.log 爆破组（grep -c Failed/Accepted）
- 纠错：`2> /dev/null` 写成 `2 > /dev/null` → find 把 "2" 当路径、结果全进黑洞