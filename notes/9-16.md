# 2026-09-16 notes

- 学了：Linux 命令 12 条（cat/head/tail/grep、mv 改名、rm -r）；for 读文件是按 `\n` 切行，切几段转几圈
<!-- 1. for 到底怎么"读"文件
- for 不负责任"读"，它只负责"接住下一个"。
- 每次拿多大，由右边的对象自己定：列表 = 元素，字符串 = 字符，文件 = 行。
- 文件的"行"是 Python 自己加的规则：读到 \n 就切断，交出一段。 -->

- 做了：wordcount.py v2(sorted) ；Linux