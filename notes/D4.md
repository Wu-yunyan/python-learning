# D 2026-09-11

## 学了
- for/回文[::-1]
- string len()函数、s.upper()字符串功能
- import random/ random.randint()

## 卡
- has_digit = any(i.isdigit() for i in password),any() 一句话：“只要有一个满足，就返回 True”
- pyinstaller打包，.gitignore忽略上传仓库
- 双击运行的程序结束即关窗 → 结尾加 `input()` 暂停
- .gitignore 只拦未跟踪的文件；已提交的要 git rm -r --cached 除名

## 坑
- find 返回下标不能当 True 用，判断"含有"用  `for i in password:` 逐字符查 + `in`；
- `is-`结果"全是或全不是"

