# W1 周复盘 2026-09-13

## 本周产出
- 环境：Python 3.13 + Git + VS Code + GitHub 全通
- 代码：info.py / grade.py / password_checker.py / guess.py（打包 exe）/ quadratic.py
- 笔记：D1–D4 共 4 篇，仓库 15 个文件，绿墙连续 4 天

## 本周踩过的坑（最有价值）
- `==` 写成比较不报错但 flag 永远为 0——不报错的 bug 最难查
- sqrt(负数) 直接崩 → 先算判别式再开方
- input() 永远是字符串，进函数前先转
- .gitignore 空文件等于没建；已跟踪的要 git rm --cached 除名

## 下周（W2）目标
- 函数收尾：password_checker 函数化 + stats.py 成绩统计器
- 开启 Linux：虚拟机 + 命令行 + shell 基础